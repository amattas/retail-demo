# Azure Event Hub / Microsoft Fabric RTI Connection
# =================================================
#
# Obtain connection string from:
# - Azure Portal: Event Hubs namespace -> Shared access policies
# - Microsoft Fabric: Real-Time Intelligence -> Eventstream -> Details
#
# Format:
# Endpoint=sb://your-namespace.servicebus.windows.net/;SharedAccessKeyName=your-key-name;SharedAccessKey=your-key;EntityPath=your-hub
#
# IMPORTANT: Never commit this file with actual credentials
AZURE_EVENTHUB_CONNECTION_STRING=

# Optional: Azure Key Vault Configuration
# ========================================
# Alternative to storing connection string in environment variable
# Requires: pip install azure-keyvault-secrets azure-identity
#
# AZURE_KEYVAULT_URL=https://your-vault.vault.azure.net/
# AZURE_KEYVAULT_SECRET_NAME=eventhub-connection-string

# Application Configuration
# =========================
# Optional: Override CORS allowed origins (comma-separated)
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Optional: Set log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Optional: Azure Storage (Blob/Data Lake) for uploads
# ================================================
# If you plan to upload generated data to Azure Storage, set either via env
# or config.json `storage` section. These are not required for local export.
# AZURE_STORAGE_ACCOUNT_URI=https://youraccount.blob.core.windows.net
# AZURE_STORAGE_ACCOUNT_KEY=your-storage-account-key
