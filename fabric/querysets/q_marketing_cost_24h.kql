// Marketing cost per campaign in last 24h
fn_campaign_performance(24h)
| summarize impressions = sum(impressions),
            spend_cents = sum(spend_cents)
    by campaign_id
| extend cost = todouble(spend_cents) / 100.0
| order by spend_cents desc
